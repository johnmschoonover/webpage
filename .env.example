# Environment template for theschoonover.net
# Copy this file to `.env` and fill in the values before running the site locally or deploying.

# -----------------------------------------------------------------------------
# Core site configuration
# -----------------------------------------------------------------------------
# Public URL where the site is hosted. Use the local dev server URL when running locally.
SITE_URL=http://localhost:4321

# Optional display name for transactional emails.
SITE_NAME=theschoonover.net

# -----------------------------------------------------------------------------
# SMTP email relay (required for the contact form)
# -----------------------------------------------------------------------------
# Hostname or IP of the SMTP relay service.
SMTP_HOST=
# Port number for the SMTP relay (587 for STARTTLS, 465 for TLS, 25 for plain SMTP).
SMTP_PORT=587
# Username for the SMTP relay authentication.
SMTP_USER=
# Password or app-specific token for SMTP authentication.
SMTP_PASS=
# Email address that will appear in the "From" header of outbound messages.
SMTP_FROM=no-reply@theschoonover.net
# Email address to receive contact form submissions (can match SMTP_FROM).
CONTACT_RECIPIENT=hello@theschoonover.net
# Set to "true" to force TLS immediately on connect (port 465). Use "false" for STARTTLS.
SMTP_SECURE=false

# -----------------------------------------------------------------------------
# hCaptcha configuration (required in production for spam prevention)
# -----------------------------------------------------------------------------
# Public site key from hCaptcha. Use the "10000000-ffff-ffff-ffff-000000000001" test key for local development.
HCAPTCHA_SITEKEY=
# Secret key from hCaptcha used for server-side verification.
HCAPTCHA_SECRET=
# Optional override for the hCaptcha verification endpoint (leave blank for default).
HCAPTCHA_ENDPOINT=

# -----------------------------------------------------------------------------
# Plausible Analytics (optional)
# -----------------------------------------------------------------------------
# Enable Plausible analytics tracking script when set to "true".
PLAUSIBLE_ENABLED=false
# Base domain you want Plausible to track (usually matches SITE_URL host).
PLAUSIBLE_DOMAIN=theschoonover.net
# Host serving the Plausible script. Use your self-hosted domain or https://plausible.io.
PLAUSIBLE_SCRIPT_HOST=https://plausible.io
# Optional API host for the Plausible event endpoint.
PLAUSIBLE_API_HOST=https://plausible.io

# -----------------------------------------------------------------------------
# Logging & diagnostics (optional)
# -----------------------------------------------------------------------------
# One of: debug, info, warn, error. Defaults to info if unset.
LOG_LEVEL=info
# Optional Sentry/monitoring DSN placeholder if instrumentation is added later.
SENTRY_DSN=
