---
const { title, description } = Astro.props;
---
<div class="flex flex-col gap-1 rounded-xl border border-border bg-background/80 p-6 text-left shadow-sm backdrop-blur">
  <p class="text-sm uppercase tracking-wide text-muted-foreground">{description}</p>
  <p class="font-display text-2xl font-semibold text-foreground">{title}</p>
</div>
